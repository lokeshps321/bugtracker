# Streamlit Cloud Secrets Configuration
# Copy this content to Streamlit Cloud → App Settings → Secrets

API_URL = "http://localhost:8000"
# After deploying backend, replace with: https://your-backend.onrender.com
